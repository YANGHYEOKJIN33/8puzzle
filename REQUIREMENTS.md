# 8-퍼즐 인공지능 탐색 알고리즘 학습 사이트 — 요구사항 정의서

- **문서 버전**: v1.9
- **작성일**: 2026-09-02
- **문서 상태**: 확정 (10장 확정 사항 반영)
- **저장소**: `yanghyeokjin33/Alg`
- **개발 브랜치**: `claude/8-puzzle-ai-visualization-363eh2`

---

## 1. 프로젝트 개요

### 1.1 한 줄 요약

파이썬을 잘 모르는 학생도 **8-퍼즐**을 소재로 인공지능 기초 교과서의 **탐색 알고리즘**을
순서도 → 의사코드 → 파이썬 코드 순으로 단계적으로 학습할 수 있는,
**자료구조 변화를 실시간으로 시각화**하는 웹 사이트.

### 1.2 배경 및 문제 인식

인공지능 기초 과목에서 탐색 알고리즘(DFS, BFS, 최상우선탐색, 언덕등반 등)을 가르칠 때
학생들이 겪는 어려움은 크게 두 가지다.

1. **자료구조가 보이지 않는다.** OPEN 리스트가 큐인지 스택인지 우선순위 큐인지에 따라
   알고리즘의 성격이 완전히 달라지는데, 교과서의 정적인 그림만으로는 그 변화를 체감하기 어렵다.
2. **파이썬 문법의 장벽.** 알고리즘을 이해하기도 전에 문법 오류와 씨름하다가
   정작 배워야 할 "탐색의 원리"에 도달하지 못한다.

이 프로젝트는 **파이썬 문법을 몰라도 알고리즘의 동작을 먼저 체험**하게 하고,
학생의 준비 정도에 따라 **점진적으로 코딩 부담을 높이는** 방식으로 두 문제를 동시에 해결한다.

### 1.3 목표

| 구분 | 목표 |
|---|---|
| 학습 목표 | 학생이 탐색 알고리즘의 원리와 자료구조의 역할을 스스로 설명할 수 있다 |
| 체험 목표 | 학생이 코드 한 줄 한 줄이 자료구조를 어떻게 바꾸는지 눈으로 확인한다 |
| 코딩 목표 | 학생이 최종적으로 빈 화면에서 탐색 알고리즘을 파이썬으로 작성할 수 있다 |
| 운영 목표 | 설치 없이 브라우저 주소만으로 접속하여 온라인 학습이 가능하다 |

### 1.4 범위 밖(Out of Scope)

- 로그인, 회원가입, 학생별 성적 관리 서버 (초기 버전은 서버 없는 정적 사이트)
- 8-퍼즐 이외의 문제(선교사와 식인종, 하노이 탑 등) — 향후 확장 여지만 남긴다
- 기계학습/신경망 관련 내용 — 이 사이트는 **탐색(Search)** 에 집중한다

---

## 2. 대상 사용자

### 2.1 주 사용자 — 학생

- **고등학교** 인공지능 기초 수강생 (확정)
- 파이썬 경험: **없음 ~ 초급**(변수, if, for 정도를 들어본 수준)
- 사용 환경: 학교 컴퓨터실 PC 또는 개인 노트북의 웹 브라우저
- 요구: "설치 없이", "복잡한 설정 없이", "눌러보면 알 수 있게"

### 2.2 보조 사용자 — 교사

- 수업 중 화면에 띄워 시연하거나, 과제로 특정 단계를 지정
- 요구: 알고리즘/초기 상태를 빠르게 바꿔가며 시연, 학생이 헤매지 않는 단순한 화면

### 2.3 사용자 시나리오

1. 학생이 사이트에 접속한다.
2. 알고리즘 목록에서 "너비 우선 탐색"을 고른다.
3. **1단계(의사코드)** 에서 `다음 버튼`을 누르며, 의사코드의 어느 줄이 실행 중인지,
   OPEN 큐에 어떤 상태가 들어오고 나가는지를 관찰한다.
4. **2단계(빈칸 채우기)** 로 넘어가 `OPEN.pop(___)` 의 빈칸을 채운다.
   `0`을 넣으면 BFS, `-1`을 넣으면 DFS가 되는 것을 직접 확인한다.
5. **3단계(직접 작성)** 에서 빈 편집기에 BFS 함수를 처음부터 작성하고,
   자신의 코드가 만드는 탐색 과정을 그대로 시각화해 본다.

---

## 3. 학습 콘텐츠 요구사항

### 3.1 문제 정의: 8-퍼즐

- 3×3 격자에 1~8의 타일과 하나의 빈칸(0)이 놓인 상태
- 빈칸과 상하좌우로 인접한 타일을 빈칸으로 밀어 넣는 것이 하나의 연산
- 초기 상태에서 목표 상태에 도달하는 **연산의 순서**를 찾는 것이 문제
- 기본 목표 상태

  ```
  1 2 3
  4 5 6
  7 8 _
  ```

- **요구사항 3.1.1**: 초기 상태를 사용자가 직접 입력하거나, 난이도별 예제
  (쉬움: 5수 이내 / 보통: 10수 내외 / 어려움: 20수 이상)에서 고를 수 있어야 한다.
- **요구사항 3.1.2**: 해가 존재하지 않는 배치(역위 수 홀수)는 입력 시점에 안내해야 한다.
- **요구사항 3.1.3**: 목표 상태도 변경할 수 있어야 한다(기본값은 위 배치).

### 3.2 구현할 알고리즘 목록

인공지능 기초 교과서에 수록된 탐색 기법을 **맹목적 탐색**과 **경험적 탐색**으로 나누어 제공한다.

#### (가) 맹목적 탐색 (Blind / Uninformed Search)

| # | 알고리즘 | 핵심 자료구조 | 학습 포인트 |
|---|---|---|---|
| 1 | 너비 우선 탐색 (BFS) | 큐 (FIFO) | 최단 경로 보장, 메모리 폭증 |
| 2 | 깊이 우선 탐색 (DFS) | 스택 (LIFO) | 메모리 절약, 최적해 미보장, 무한 깊이 위험 |
| 3 | 깊이 제한 탐색 (DLS) | 스택 + 깊이 한계 | 깊이 제한의 필요성 |
| 4 | 반복적 깊이 심화 탐색 (IDS) | 스택 + 반복 | BFS의 최적성과 DFS의 메모리를 함께 |

#### (나) 경험적 탐색 (Heuristic / Informed Search)

| # | 알고리즘 | 핵심 자료구조 | 평가 함수 | 학습 포인트 |
|---|---|---|---|---|
| 5 | 언덕 등반 (Hill Climbing) | 현재 상태 1개 | `h(n)` | 지역 최적, 되돌아가지 않음 |
| 6 | 최상 우선 탐색 (Best-First, Greedy) | 우선순위 큐 | `h(n)` | 빠르지만 최적 미보장 |
| 7 | A* 알고리즘 | 우선순위 큐 | `f(n)=g(n)+h(n)` | 허용적 휴리스틱과 최적성 |
| 8 | 균일 비용 탐색 (UCS) | 우선순위 큐 | `g(n)` | A*와 BFS 사이의 위치 |

- **요구사항 3.2.1**: 최소 1~7번을 제공한다. 8번은 선택 구현.
- **요구사항 3.2.2**: 휴리스틱 함수는 학생이 **선택**할 수 있어야 한다.
  - `h1`: 제자리에 없는 타일의 개수
  - `h2`: 맨해튼 거리의 합
  - `h0`: 항상 0 (→ A*가 UCS가 되는 것을 관찰)
- **요구사항 3.2.3**: 같은 초기 상태에 대해 **두 알고리즘의 성능을 나란히 비교**하는
  화면을 제공한다(방문 노드 수, 최대 OPEN 크기, 해의 길이, 소요 단계).

### 3.3 3단 학습 표현 (순서도 → 의사코드 → 파이썬)

모든 알고리즘은 아래 세 가지 표현을 **모두** 제공하며, 서로 대응 관계가 드러나야 한다.

- **요구사항 3.3.1 (순서도)**: 알고리즘의 제어 흐름을 도형(시작/처리/판단/종료)으로 표현한다.
  실행 중에는 **현재 위치한 도형이 강조**된다.
- **요구사항 3.3.2 (의사코드)**: 교과서 표기에 가까운 한글 의사코드를 제공한다.
  실행 중에는 **현재 실행 중인 줄이 강조**된다.
- **요구사항 3.3.3 (파이썬 코드)**: 의사코드와 **줄 단위로 대응되는** 파이썬 코드를 제공한다.
  실행 중에는 현재 실행 중인 줄이 강조된다.
- **요구사항 3.3.4 (동시 보기)**: 순서도 / 의사코드 / 파이썬을 탭으로 전환하거나,
  최소한 "의사코드 ↔ 파이썬" 두 표현을 나란히 볼 수 있어야 한다.

---

## 4. 시각화 요구사항 (핵심 기능)

> "코드 진행 상황에 따라 어떻게 자료구조가 변화하는지 학생이 보고 직접 체험한다."

### 4.1 화면에 항상 보여야 하는 4가지 패널

| 패널 | 내용 |
|---|---|
| ① 퍼즐 보드 | 현재 검사 중인 노드의 3×3 배치. 직전 상태에서 움직인 타일을 강조 |
| ② 코드 패널 | 의사코드 또는 파이썬 코드. 현재 실행 줄 하이라이트 |
| ③ 자료구조 패널 | OPEN / CLOSED의 내용물을 그림으로 표시 |
| ④ 탐색 트리 패널 | 지금까지 펼쳐진 노드의 트리. 현재 노드/해 경로 강조 |

### 4.2 자료구조 시각화 세부 요구사항

- **요구사항 4.2.1**: OPEN 리스트는 알고리즘에 따라 **모양이 달라져야 한다.**
  - 큐(BFS): 왼쪽에서 나가고 오른쪽으로 들어오는 가로 띠
  - 스택(DFS): 위에서 넣고 위에서 빼는 세로 더미
  - 우선순위 큐(Best-First / A*): 평가값 순으로 정렬된 목록, 각 항목에 `f = g + h` 표시
- **요구사항 4.2.2**: 삽입(push)과 제거(pop)는 **애니메이션**으로 구분되어야 한다.
  (예: 삽입은 초록 계열로 나타나며 들어오고, 제거는 회색으로 빠져나간다)
- **요구사항 4.2.3**: CLOSED(방문 완료) 집합의 크기와 내용도 확인할 수 있어야 한다.
- **요구사항 4.2.4**: OPEN의 각 항목에 마우스를 올리면 **그 상태의 3×3 배치**가
  미리보기로 표시된다.
- **요구사항 4.2.5**: 아래 카운터를 항상 표시한다 —
  `생성한 노드 수`, `펼친(확장한) 노드 수`, `OPEN 최대 크기`, `현재 깊이`.

### 4.3 실행 제어 요구사항

- **요구사항 4.3.1**: `▶ 재생`, `⏸ 일시정지`, `⏭ 한 단계`, `⏮ 한 단계 뒤로`, `⏹ 처음으로`
  를 제공한다. **뒤로 가기는 필수**다 — 학생은 놓친 장면을 다시 봐야 한다.
- **요구사항 4.3.2**: 재생 속도를 조절할 수 있다(느림 / 보통 / 빠름).
- **요구사항 4.3.3**: "해를 찾을 때까지 건너뛰기" 버튼을 제공한다.
- **요구사항 4.3.4**: 해를 찾으면 초기 상태부터 목표까지의 **경로를 애니메이션으로 재생**한다.
- **요구사항 4.3.5**: 무한 루프 방지를 위해 최대 확장 노드 수(기본 20,000)에 도달하면
  중단하고 "탐색 공간이 너무 큽니다"를 안내한다.

---

## 5. 3단계 학습 모드 (요구사항 4번에 대응)

동일한 알고리즘을 **난이도가 다른 세 가지 모드**로 학습한다. 모드 전환은 화면 상단 한 곳에서 한다.

### 5.1 1단계 — 의사코드로 동작하는 단계 (코드 작성 없음)

- 학생은 코드를 **전혀 쓰지 않는다.** 버튼만 누른다.
- 의사코드가 한 줄씩 실행되고, 그에 맞춰 자료구조 패널이 변한다.
- **요구사항 5.1.1**: 각 단계마다 한 문장 해설을 표시한다.
  (예: "OPEN의 맨 앞에서 노드를 꺼냅니다. 큐이므로 가장 먼저 들어온 노드입니다.")
- **요구사항 5.1.2**: 중간중간 **예측 퀴즈**를 낸다.
  (예: "다음에 꺼내질 노드는? ① A ② B ③ C" → 선택 후 정답과 이유를 확인)
- **요구사항 5.1.3**: 이 단계는 파이썬 실행기를 사용하지 않는다(순수 JavaScript 시뮬레이터).
  → 로딩이 즉시 이루어져야 한다.

### 5.2 2단계 — 파이썬 코드를 채워 넣어 동작하는 단계 (빈칸 채우기)

- 거의 완성된 파이썬 코드에서 **핵심 부분만 빈칸**으로 비워 둔다.
- **요구사항 5.2.1**: 빈칸은 알고리즘의 성격을 결정하는 지점이어야 한다.
  - 예: `node = OPEN.pop(____)` → `0`이면 BFS, `-1`이면 DFS
  - 예: `OPEN.sort(key=lambda n: ____)` → `n.h`면 최상우선, `n.g + n.h`면 A*
  - 예: `def h(state): return ____` → 휴리스틱 직접 정의
- **요구사항 5.2.2**: 빈칸은 **드롭다운 선택 또는 짧은 입력** 중 문제에 맞는 방식을 쓴다.
  문법 오류로 학생이 좌절하지 않게 한다.
- **요구사항 5.2.3**: 제출하면 그 코드로 **실제 탐색을 실행**하고 결과를 시각화한다.
- **요구사항 5.2.4**: 틀렸을 때 "오답"이 아니라 **무엇이 달라졌는지**를 보여 준다.
  (예: "이 답으로는 깊이 우선처럼 동작합니다. 해의 길이가 31이 되었습니다.")

### 5.3 3단계 — 파이썬 코드를 직접 입력하여 동작하는 단계

- 학생이 편집기에 **직접 함수를 작성**한다.
- **요구사항 5.3.1**: 제공되는 도우미 API를 명확히 문서화한다.

  ```python
  # 학생에게 제공되는 것
  start          # 시작 상태 (예: (1,2,3,4,0,6,7,5,8))
  goal           # 목표 상태
  expand(state)  # 가능한 다음 상태들의 리스트를 돌려줌
  h1(state)      # 제자리에 없는 타일 수
  h2(state)      # 맨해튼 거리 합

  # 학생이 작성해야 하는 것
  def search(start, goal):
      ...
      return path   # 상태들의 리스트, 못 찾으면 None
  ```

- **요구사항 5.3.2**: 학생 코드가 실행되는 동안에도 **자료구조 변화가 시각화**되어야 한다.
  (제공 자료구조 `OpenList` 사용 시 자동 추적, 또는 코드 계측을 통해 추적)
- **요구사항 5.3.3**: 오류 발생 시 **한국어로 친절한 설명**을 덧붙인다.
  (예: `IndexError: pop from empty list` → "OPEN이 비었는데 꺼내려 했습니다.
  꺼내기 전에 비었는지 검사했나요?")
- **요구사항 5.3.4**: 무한 루프에 대비해 실행 시간/단계 수 상한을 두고 안전하게 중단한다.
- **요구사항 5.3.5**: 정답 코드 보기(단, 스스로 한 번 실행한 뒤에 열람 가능)를 제공한다.
- **요구사항 5.3.6**: 학생이 작성한 코드는 **브라우저에 자동 저장**되어
  새로고침해도 사라지지 않아야 한다.

### 5.4 단계 간 이동

- **요구사항 5.4.1**: 세 단계는 자유롭게 오갈 수 있다(잠금 없음).
  단, 권장 순서를 화면에 명확히 표시한다.
- **요구사항 5.4.2**: 단계가 바뀌어도 **선택한 알고리즘과 초기 상태는 유지**된다.

---

## 6. UI/UX 요구사항

> "UI는 단순해야 한다. 복잡하면 학생 접근성이 떨어진다."

### 6.1 단순성 원칙

- **요구사항 6.1.1**: 첫 화면에서 학생이 내려야 할 결정은 **최대 2개**
  (① 어떤 알고리즘 ② 어떤 단계). 나머지는 모두 기본값으로 동작한다.
- **요구사항 6.1.2**: 화면에 동시에 보이는 **주 버튼은 7개 이하**로 제한한다.
  고급 설정(최대 노드 수, 목표 상태 변경 등)은 "설정 더보기"에 접어 둔다.
- **요구사항 6.1.3**: 모든 문구는 **한국어**를 기본으로 하며, 전문 용어는
  첫 등장 시 괄호로 원어를 병기한다(예: 최상 우선 탐색(Best-First Search)).
- **요구사항 6.1.4**: 색상은 **의미를 가진 4색 이내**로 제한한다.
  - 파랑 = 현재 노드 / 초록 = OPEN 삽입 / 회색 = CLOSED / 주황 = 해 경로
- **요구사항 6.1.5**: 색만으로 정보를 구분하지 않는다(색맹 대응: 아이콘·라벨 병기).
- **요구사항 6.1.6**: 튜토리얼은 문서가 아니라 **첫 방문 시 3단계 안내 말풍선**으로 대체한다.

### 6.2 레이아웃

- 데스크톱: 왼쪽 = 코드/의사코드, 오른쪽 위 = 퍼즐 보드, 오른쪽 아래 = 자료구조/트리
- 태블릿·모바일: 세로 1열로 접히고, 패널은 탭으로 전환
- **요구사항 6.2.1**: 1280×720(교실 프로젝터 해상도)에서 **스크롤 없이** 핵심이 보여야 한다.
- **요구사항 6.2.2**: 폰트 크기를 키우는 버튼을 제공한다(교실 뒷자리 가독성).

### 6.3 접근성

- **요구사항 6.3.1**: 키보드만으로 실행 제어가 가능해야 한다
  (`Space`=재생/정지, `→`=한 단계, `←`=뒤로).
- **요구사항 6.3.2**: 명암비는 WCAG AA(4.5:1) 이상.
- **요구사항 6.3.3**: 다크 모드에서도 정상적으로 보여야 한다.

---

## 7. 기술 요구사항

### 7.1 배포 방식

- **요구사항 7.1.1**: **GitHub Pages** 로 배포하는 정적 사이트로 만든다.
  (서버 없음, 데이터베이스 없음, 별도 비용 없음)
- **요구사항 7.1.2**: 저장소의 지정 브랜치에 push하면 GitHub Actions로 자동 배포된다.
- **요구사항 7.1.3**: 학생은 **주소 하나**로 접속하며 로그인·설치가 필요 없다.

### 7.2 기술 스택 (제안)

| 영역 | 선택 | 이유 |
|---|---|---|
| 기반 | HTML + CSS + JavaScript (ES Modules) | 빌드 도구 없이도 GitHub Pages에서 그대로 동작 |
| 알고리즘 시뮬레이터 (1·2단계) | 순수 JavaScript 제너레이터 | 즉시 로딩, 한 단계씩 실행/되감기에 적합 |
| 파이썬 실행 (3단계) | Pyodide (CDN, 지연 로딩) | 브라우저에서 진짜 파이썬 실행, 서버 불필요 |
| 코드 편집기 | CodeMirror 6 | 가볍고 한국어 입력·문법 강조 지원 |
| 순서도 | 인라인 SVG | 의존성 없이 도형 강조 제어 가능 |

- **요구사항 7.2.1**: Pyodide는 **3단계에 진입할 때만** 내려받는다
  (1·2단계 사용자가 수십 MB를 기다리게 하지 않는다).
- **요구사항 7.2.2**: 프레임워크(React 등) 도입은 필수가 아니며,
  도입하더라도 **빌드 결과물이 GitHub Pages에서 그대로 열려야 한다.**

### 7.3 성능 요구사항

- **요구사항 7.3.1**: 첫 화면 표시까지 **3초 이내**(학교 네트워크 기준).
- **요구사항 7.3.2**: 한 단계 실행의 화면 갱신은 **100ms 이내**.
- **요구사항 7.3.3**: 탐색 실행이 브라우저를 멈추게 해서는 안 된다
  (오래 걸리는 탐색은 분할 실행 또는 Web Worker 사용).

### 7.4 호환성

- **요구사항 7.4.1**: 최신 Chrome / Edge / Safari / Firefox를 지원한다.
- **요구사항 7.4.2**: 인터넷 익스플로러는 지원하지 않는다.

### 7.5 코드 품질

- **요구사항 7.5.1**: 알고리즘 로직과 화면 표시 코드는 분리한다
  (알고리즘은 "단계 목록"을 만들고, 화면은 그것을 그리기만 한다).
- **요구사항 7.5.2**: 각 알고리즘의 정확성을 검증하는 테스트를 둔다
  (예: BFS의 해 길이 ≤ DFS의 해 길이, A*는 항상 최적해).
- **요구사항 7.5.3**: 새 알고리즘을 추가할 때 **한 파일만 추가**하면 되도록 설계한다.

---

## 8. 개발 단계 (로드맵)

| 단계 | 산출물 | 상태 |
|---|---|---|
| 1단계 | 본 요구사항 문서(`REQUIREMENTS.md`) 작성 | ✅ 완료 |
| 2단계 | 저장소 정리, 사이트 뼈대, GitHub Pages 자동 배포 구성 | ✅ 완료 |
| **3단계** | **8-퍼즐 코어 엔진(상태 표현, 연산, 휴리스틱) + 테스트** | **✅ 완료** |
| **4단계** | **알고리즘 단계 기록기(step trace) — BFS/DFS 우선** | **✅ 완료** |
| **5단계** | **시각화 화면(퍼즐 보드·자료구조·실행 제어) — BFS·DFS** | **✅ 완료** |
| **6단계** | **학습 1단계(의사코드 모드) 다듬기 + 순서도** | **✅ 완료** |
| **7단계** | **나머지 알고리즘(DLS, IDS, 언덕등반, 최상우선, A*) 추가** | **✅ 완료** |
| **8단계** | **학습 2단계(빈칸 채우기)** | **✅ 완료** |
| **9단계** | **학습 3단계(Pyodide 직접 작성)** | **✅ 완료** |
| **10단계** | **알고리즘 비교 화면, 예측 퀴즈** | **✅ 완료** |
| 11단계 | 접근성·모바일·다크 모드 마무리, 교사용 안내 문서 | 다음 |

---

## 9. 완료 기준 (Definition of Done)

프로젝트는 아래를 **모두** 만족할 때 완료로 본다.

1. 학생이 브라우저 주소만으로 접속하여 설치 없이 학습할 수 있다.
2. 최소 6종의 탐색 알고리즘(BFS, DFS, DLS, IDS, 언덕등반, 최상우선, A*)이 동작한다.
3. 모든 알고리즘에 대해 순서도·의사코드·파이썬 코드가 제공되고 서로 대응된다.
4. 실행 중 OPEN/CLOSED의 변화가 알고리즘별로 다른 모습으로 시각화된다.
5. 한 단계씩 앞으로/뒤로 이동할 수 있다.
6. 3단계 학습 모드가 모두 동작하며, 3단계에서 학생이 쓴 파이썬 코드가 실제로 실행된다.
7. 1280×720 화면에서 스크롤 없이 핵심 정보가 보인다.
8. 파이썬을 처음 접하는 학생이 **안내 없이 5분 안에** 첫 탐색 애니메이션을 볼 수 있다.

---

## 10. 확정 사항

아래 항목은 2026-09-02 확정되었다.

| # | 항목 | 확정 내용 |
|---|---|---|
| Q1 | 사이트 언어 | 한국어 전용 (영어는 용어 병기 수준) |
| Q2 | 대상 학년 | **고등학생** |
| Q3 | A* 포함 여부 | **A\* 포함**, 균일 비용 탐색(UCS)은 선택 구현 |
| Q4 | 학습 진도 저장 | 브라우저 로컬 저장만 (서버 없음) |
| Q5 | 8-퍼즐 외 문제 확장 | 이번 범위 제외, 구조만 확장 가능하게 |
| Q6 | 교사용 기능(과제 링크 생성 등) | **추후 과제로 미룸** (이번 범위 제외) |

확정에 따라 문구를 다음과 같이 맞춘다.

- 모든 설명·용어·예시의 눈높이는 **고등학생** 기준으로 작성한다.
  대학 수준의 형식적 증명(예: A\*의 최적성 증명)은 다루지 않고,
  "왜 그런지"를 예시와 시각화로 납득시키는 방식을 택한다.
- A\*는 필수 구현 대상이며, 휴리스틱 `h0`(항상 0)을 골랐을 때
  A\*가 균일 비용 탐색과 같아지는 것을 관찰하는 방식으로 UCS를 대신할 수 있다.

---

## 부록 A. 용어 정리

| 용어 | 설명 |
|---|---|
| 상태(State) | 8-퍼즐 타일의 한 배치 |
| 노드(Node) | 상태 + 부모 + 깊이 + 평가값을 묶은 탐색 단위 |
| 연산자(Operator) | 빈칸을 상/하/좌/우로 옮기는 행동 |
| OPEN | 아직 펼치지 않은, 검사 대기 중인 노드의 모음 |
| CLOSED | 이미 펼친(확장한) 노드의 모음 |
| 확장(Expand) | 한 노드에서 가능한 자식 노드들을 만들어 내는 일 |
| g(n) | 시작 노드에서 n까지 실제로 든 비용(= 깊이) |
| h(n) | n에서 목표까지의 예상 비용(휴리스틱) |
| f(n) | g(n) + h(n), A*의 평가 함수 |
| 허용적 휴리스틱 | 실제 비용을 절대 넘겨 잡지 않는 h. A*의 최적성 조건 |
